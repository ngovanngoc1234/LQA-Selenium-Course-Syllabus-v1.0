var Symbol = require('./_Symbol');

/** Used to convert symbols to primitives and strings. */
var symbolProto = Symbol ? Symbol.prototype : undefined,
    symbolValueOf = symbolProto ? symbolProto.valueOf : undefined;

/**
 * Creates a clone of the `symbol` com.example.demoselenium.object.
 *
 * @private
 * @param {Object} symbol The symbol com.example.demoselenium.object to clone.
 * @returns {Object} Returns the cloned symbol com.example.demoselenium.object.
 */
function cloneSymbol(symbol) {
  return symbolValueOf ? Object(symbolValueOf.call(symbol)) : {};
}

module.exports = cloneSymbol;
