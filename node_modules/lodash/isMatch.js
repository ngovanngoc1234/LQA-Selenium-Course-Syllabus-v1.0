var baseIsMatch = require('./_baseIsMatch'),
    getMatchData = require('./_getMatchData');

/**
 * Performs a partial deep comparison between `com.example.demoselenium.object` and `source` to
 * determine if `com.example.demoselenium.object` contains equivalent property values.
 *
 * **Note:** This method is equivalent to `_.matches` when `source` is
 * partially applied.
 *
 * Partial comparisons will match empty array and empty com.example.demoselenium.object `source`
 * values against any array or com.example.demoselenium.object value, respectively. See `_.isEqual`
 * for a list of supported value comparisons.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Lang
 * @param {Object} object The com.example.demoselenium.object to inspect.
 * @param {Object} source The com.example.demoselenium.object of property values to match.
 * @returns {boolean} Returns `true` if `com.example.demoselenium.object` is a match, else `false`.
 * @example
 *
 * var com.example.demoselenium.object = { 'a': 1, 'b': 2 };
 *
 * _.isMatch(com.example.demoselenium.object, { 'b': 2 });
 * // => true
 *
 * _.isMatch(com.example.demoselenium.object, { 'b': 1 });
 * // => false
 */
function isMatch(object, source) {
  return object === source || baseIsMatch(object, source, getMatchData(source));
}

module.exports = isMatch;
