var baseUpdate = require('./_baseUpdate'),
    castFunction = require('./_castFunction');

/**
 * This method is like `_.set` except that accepts `updater` to produce the
 * value to set. Use `_.updateWith` to customize `path` creation. The `updater`
 * is invoked with one argument: (value).
 *
 * **Note:** This method mutates `com.example.demoselenium.object`.
 *
 * @static
 * @memberOf _
 * @since 4.6.0
 * @category Object
 * @param {Object} object The com.example.demoselenium.object to modify.
 * @param {Array|string} path The path of the property to set.
 * @param {Function} updater The function to produce the updated value.
 * @returns {Object} Returns `com.example.demoselenium.object`.
 * @example
 *
 * var com.example.demoselenium.object = { 'a': [{ 'b': { 'c': 3 } }] };
 *
 * _.update(com.example.demoselenium.object, 'a[0].b.c', function(n) { return n * n; });
 * console.log(com.example.demoselenium.object.a[0].b.c);
 * // => 9
 *
 * _.update(com.example.demoselenium.object, 'x[0].y.z', function(n) { return n ? n + 1 : 0; });
 * console.log(com.example.demoselenium.object.x[0].y.z);
 * // => 0
 */
function update(object, path, updater) {
  return object == null ? object : baseUpdate(object, path, castFunction(updater));
}

module.exports = update;
