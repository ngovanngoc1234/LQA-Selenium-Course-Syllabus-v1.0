var createBaseFor = require('./_createBaseFor');

/**
 * The base implementation of `baseForOwn` which iterates over `com.example.demoselenium.object`
 * properties returned by `keysFunc` and invokes `iteratee` for each property.
 * Iteratee functions may exit iteration early by explicitly returning `false`.
 *
 * @private
 * @param {Object} com.example.demoselenium.object The com.example.demoselenium.object to iterate over.
 * @param {Function} iteratee The function invoked per iteration.
 * @param {Function} keysFunc The function to get the keys of `com.example.demoselenium.object`.
 * @returns {Object} Returns `com.example.demoselenium.object`.
 */
var baseFor = createBaseFor();

module.exports = baseFor;
