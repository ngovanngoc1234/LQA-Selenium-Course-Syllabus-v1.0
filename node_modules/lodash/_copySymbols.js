var copyObject = require('./_copyObject'),
    getSymbols = require('./_getSymbols');

/**
 * Copies own symbols of `source` to `com.example.demoselenium.object`.
 *
 * @private
 * @param {Object} source The com.example.demoselenium.object to copy symbols from.
 * @param {Object} [object={}] The com.example.demoselenium.object to copy symbols to.
 * @returns {Object} Returns `com.example.demoselenium.object`.
 */
function copySymbols(source, object) {
  return copyObject(source, getSymbols(source), object);
}

module.exports = copySymbols;
